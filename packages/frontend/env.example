# Variables de entorno para SaluData Frontend
# Copia este archivo como .env.local y actualiza los valores

# Dirección del contrato SaluData (actualizar después del deploy)
NEXT_PUBLIC_SALUDATA_CONTRACT_ADDRESS=

# ID del proyecto de WalletConnect (opcional)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Red por defecto (localhost, sepolia, mainnet)
NEXT_PUBLIC_DEFAULT_CHAIN=localhost
